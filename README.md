# Terminal Music Player

A simple, interactive command-line application that plays `.mp3` files from a local directory.

## How to run

1. Place your `.mp3` files inside the `songs` folder.
2. Open your terminal in the root of this project.
3. Run the application using Node:

node index.js